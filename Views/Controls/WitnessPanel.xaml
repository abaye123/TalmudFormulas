<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="TalmudFormulas.Views.Controls.WitnessPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border x:Name="HeaderBorder"
                Grid.Row="0"
                Padding="18,12,18,12"
                BorderThickness="0,0,0,1"
                Background="Transparent"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
            <StackPanel Spacing="10">
                <TextBlock x:Name="HeaderLabel"
                           Text="בחר קטע לעדי נוסח"
                           FontSize="15"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Right"
                           Foreground="{ThemeResource TextFillColorPrimaryBrush}" />

                <StackPanel Orientation="Horizontal"
                            Spacing="14"
                            HorizontalAlignment="Right">
                    <CheckBox x:Name="HideMinorCheck"
                              Content="הסתר שינויים קלים"
                              FontSize="12"
                              Checked="OnHideMinorChanged"
                              Unchecked="OnHideMinorChanged"
                              IsEnabled="False" />
                    <CheckBox x:Name="HideEmptyCheck"
                              Content="הסתר עדים ריקים"
                              FontSize="12"
                              Checked="OnHideEmptyChanged"
                              Unchecked="OnHideEmptyChanged" />
                    <CheckBox x:Name="HighlightCheck"
                              Content="הדגש שינויים מוילנא"
                              FontSize="12"
                              Checked="OnHighlightChanged"
                              Unchecked="OnHighlightChanged" />
                </StackPanel>

                <!-- באנר רמז סולידי במקום טקסט נטוי משוחרר -->
                <Border x:Name="HintLabel"
                        CornerRadius="6"
                        Padding="10,6"
                        HorizontalAlignment="Stretch"
                        Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                        BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Stretch">
                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}"
                                  Glyph="&#xE946;"
                                  FontSize="12"
                                  VerticalAlignment="Center"
                                  Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                        <TextBlock x:Name="HintLabelText"
                                   Text="לחיצה על קטע תציג את השינויים בטקסט המרכזי"
                                   FontSize="11"
                                   VerticalAlignment="Center"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <!-- BODY -->
        <ScrollViewer x:Name="WitnessScroll"
                      Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Background="Transparent">
            <StackPanel x:Name="WitnessContainer"
                        Padding="6,12,6,24"
                        Spacing="4" />
        </ScrollViewer>
    </Grid>
</UserControl>
